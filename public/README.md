# Public Assets

Static assets for `elowen-ui` live here when they need to be bundled by the Trunk build.

The UI is currently a client-side Leptos app shipped through a prebuilt GHCR image. Keep generated artifacts out of this directory; only source assets that should be versioned belong here.
